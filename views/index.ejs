<%- include("partials/header.ejs")-%>


<div class="container mt-5">
    <div class="row">
    <% books.forEach((e) => { %>
      <div class=" col-lg-3">
        <div class="card mr-3" ">
          <img class="card-img-top " src="<%= e.image%>" alt="Card image cap">
          <div class="card-body">
            <h5 class="card-title "><%= e.title %> <a class="btn-sm btn-primary" href="/cart/add/<%= e.id %>">Add to card</a></h5>
            <p class="card-text"><%= e.description %> </p>
            <a href="/<%= e.id %>/view" class="btn-sm btn-primary">View</a>
            <a href="/<%= e.id %>/edit" class="btn-sm btn-warning">Edit</a>
            <form action="/<%= e.id %>/delete" method="POST">
              <button class="btn-sm btn-danger mt-2">Delete</button>
            </form>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
</div>


<%- include("partials/footer.ejs")-%>